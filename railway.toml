[build]
builder = "nixpacks"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/fub-insights/api/status"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
